apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-service-dev.fullname" . }}
  labels:
    {{- include "app-service-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-service-dev.selectorLabels" . | nindent 4 }}
